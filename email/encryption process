dnf --disablerepo='*' --enablerepo=nodesource-nodejs list available nodejs --showduplicates


dnf repolist --all | grep -i node

grep -R "^\[.*\]" /etc/yum.repos.d/ | grep -i node


dnf --disablerepo='*' --enablerepo=nodesource list available nodejs --showduplicates


cat /etc/yum.repos.d/nodesource-el8.repo


sudo -n true && echo "sudo access OK" || echo "sudo requires authentication"


rpm -q nodejs


sudo -l


Hi Ryan, I’ve disabled tca02 and completed the pre-upgrade vCenter snapshot. I confirmed it’s currently running Node 18.17.1 and the NodeSource repo is configured for Node 18. I’m ready to proceed with the Node 24 upgrade, but the TeamCity teamcity account requires a password for sudo. What’s the normal process for getting elevated/root access on these build agents? Is there a privileged account I should use instead?
