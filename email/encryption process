aws configure get ca_bundle

echo $env:AWS_CA_BUNDLE

echo $env:REQUESTS_CA_BUNDLE

Invoke-WebRequest https://oidc.us-east-1.amazonaws.com



aws --version

aws configure get ca_bundle

echo $env:AWS_CA_BUNDLE

Invoke-WebRequest https://oidc.us-east-1.amazonaws.com





Were there any recent changes to outbound SSL inspection, proxy configuration, or corporate CA certificates for AWS endpoints? AWS CLI SSO login is now failing certificate validation against oidc.us-east-1.amazonaws.com with CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate.”


Test-Path C:\netskope\netskope-cert-bundle.pem

Get-Item C:\netskope\netskope-cert-bundle.pem


aws configure get ca_bundle
echo $env:AWS_CA_BUNDLE
echo $env:REQUESTS_CA_BUNDLE

Get-Item C:\netskope\netskope-cert-bundle.pem |
Select Name,Length,LastWriteTime

curl.exe -v https://oidc.us-east-1.amazonaws.com




Hi team, I’m running into an SSL certificate validation issue when attempting aws sso login on one of the Windows servers. The AWS CLI is returning CERTIFICATE_VERIFY_FAILED – unable to get local issuer certificate when connecting to the AWS OIDC endpoint.

I checked the certificate configuration and both AWS_CA_BUNDLE and REQUESTS_CA_BUNDLE are pointing to C:\netskope\netskope-cert-bundle.pem. Has anyone seen this issue recently, or are there any recent Netskope/certificate changes that could be affecting the CA bundle on the server?




WS SSO login is failing SSL certificate validation against the configured Netskope CA bundle. Connectivity to the AWS OIDC endpoint over 443 is successful, and SSO proceeds when certificate verification is bypassed, indicating the issue is with the Netskope certificate trust chain/CA bundle rather than AWS connectivity or SSO permissions.
