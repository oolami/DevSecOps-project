The DR S3 bucket has been created and configured to match the Prod bucket (versioning enabled, SSE-S3 encryption, bucket key enabled, and Block Public Access configured). The bucket is ready on the DR side.

The remaining work is to create the cross-account IAM replication role and configure the replication rule from the Prod account. I know Erik typically handles the IAM role creation, but since he's out of the office this week, is there anyone else who can assist with creating the IAM role for the S3 bucket replication so we can continue with the setup?
