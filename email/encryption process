Hi Chris,

I tested the temporary AWS Identity Center credentials by setting the AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, and AWS_SESSION_TOKEN environment variables as suggested.

Unfortunately, it does not appear to resolve the issue. When I run aws sts get-caller-identity using those credentials, the AWS CLI still fails with:

CERTIFICATE_VERIFY_FAILED – unable to get local issuer certificate

I also tested the same command with --no-verify-ssl, and it succeeds and returns my expected GMS-CloudEngDR assumed role. This confirms the temporary credentials themselves are valid, but AWS CLI certificate validation is still failing when making the HTTPS request to the AWS STS endpoint.

I also ran the certificate checks you requested. I’ve attached the PowerShell output showing the results. It looks like the Netskope certificate exists in the local root store, but the AWS CLI is still unable to validate the certificate chain.
