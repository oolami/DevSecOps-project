After reviewing the implementation with Ryan, I moved away from creating a separate Docker Swarm secret/keytab entry and switched to the existing Wholeshestack secret-generation pattern. The Nasdaq password and private key are now configured as protected TeamCity environment variables and referenced in scripts/services/market-index-api.sh, so they will be included in the existing generated market-index-api secrets file during deployment.

I also removed the previous standalone Nasdaq secret definition/mount and the additional TeamCity private-key preparation step since they are no longer needed with this approach.

The current blocker is that the deployment is still failing in Step 1 – Prepare config with:
yaml: map merge requires map or sequence of maps as the value

The failure occurs before the application deployment starts, so the new Nasdaq credential implementation has not yet been fully tested in Juliet. The next area I was investigating is the YAML merge/anchor processing in wholeshestack.sh / keytab.sh, since the Nasdaq keytab entry itself has already been removed.





Work completed / current status:

* Generated and validated the Nasdaq SSH key pair and provided the public key for Nasdaq configuration.
* Initially implemented the private key as a separate Docker Swarm secret and mounted it into market-index-api.
* Encountered YAML merge failures during Prepare config, and confirmed with Ryan that the keytab mechanism is intended for Kerberos-generated secrets and should not be used for the Nasdaq SSH key.
* Reviewed the existing Wholeshestack secret-generation process with Ryan and changed the implementation to reuse the standard application-secret pattern.
* Added the Nasdaq credentials to the market-index-api secret generation:
    * MARKET_INDEX_API_NASDAQ_PASSWORD
    * MARKET_INDEX_API_NASDAQ_PRIVATE_KEY
* Configured the private key in TeamCity as a protected/password environment parameter using the decoded OpenSSH private-key value.
* Updated scripts/services/market-index-api.sh so NasdaqOptions includes both the existing password and the new private key, allowing the application team to test the new authentication method while retaining the password as fallback.
* Removed the separate Nasdaq Docker secret declaration/mount and the dedicated TeamCity private-key preparation build step because they are no longer required.

Current blocker:
Juliet deployment is still failing during Step 1 – Prepare config with:

yaml: map merge requires map or sequence of maps as the value

The error occurs before the deployment reaches the application, so the new Nasdaq authentication configuration has not yet been validated end-to-end.





