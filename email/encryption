End-to-end workflow and branching: complete and 
validated. Internal NSX-ALB path: implemented. External Fastly path: dry-run validated; live Fastly API deployment remains the implementation step.




sudo dnf install -y dotnet-sdk-9.0

dotnet --version
dotnet --list-sdks
which dotnet




which dotnet
dotnet --version
dotnet --list-sdks


rpm -qa | grep -i dotnet


ps -ef | grep -i teamcity

systemctl list-units --type=service | grep -i teamcity



sudo -u 'teamcity@silacins.com' /usr/bin/dotnet --version

sudo -u 'teamcity@silacins.com' /usr/bin/dotnet --list-sdks

sudo -u 'teamcity@silacins.com' sh -c 'echo $PATH; command -v dotnet; dotnet --version'

sudo systemctl restart teamcity-agent.service

systemctl status teamcity-agent.service