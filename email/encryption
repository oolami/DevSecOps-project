The SQL Data Correction pipeline uses its own dedicated SQL connection configuration (doc-sqlequiflife.com) whereas the standard Data Corrections pipeline does not use these settings. Since the failure occurred during SQL connection establishment and not script execution, and given the recent AAG migration, I want to verify whether the SQL Data Corrections TeamCity configuration still points to the correct production SQL listener.
