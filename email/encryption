Investigation update:

Reviewed the existing TeamCity database restore projects for AnnuityUvIntake, IntakeApi DB, and Vertafore Agents DB.
Confirmed with the requester that AgentRewards is a separate database and that the expected restore workflow should be similar to AnnuityUvIntake/IntakeApi.
Currently waiting on additional information regarding the TeamCity configuration source (VCS repository) and the AgentRewards database details (source/target database information) before implementation can begin.
