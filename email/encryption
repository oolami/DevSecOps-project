Certificate nearing expiration
↓
TeamCity job triggered manually or by schedule/alert
↓
Script calls DigiCert API to renew existing certificate/order
↓
Script checks renewal/order status
↓
Script downloads renewed certificate once issued
↓
Certificate is passed to deployment workflow







Certificate Expiring
        ↓
DigiCert API Renewal
        ↓
Download Renewed Certificate
        ↓
NSX-ALB API Upload
        ↓
Validation



Use DigiCert APIs to automate certificate renewal and certificate retrieval. Once the certificate has been issued, separate automation workflows will deploy the certificate to the target platform, such as Fastly or NSX-ALB.




I see the WorkSpaces client is reporting high latency (276 ms) and an unhealthy network connection. Are you currently connected to VPN? Also, are you using home WiFi or a corporate network? If possible, please try disconnecting VPN and retrying, or test from a different network to see if the behavior changes.




Hi Doug, I approved the SQL Data Correction build, but it failed during execution. The log shows sqlcmd could not connect to SQL Server: Login timeout expired, TCP Provider error code 0x102, and network-related or instance-specific error occurred while establishing a connection.

It looks like a SQL connectivity/target DB access issue rather than an approval or script syntax issue. Can you please verify the SQL server/instance is reachable from the TeamCity agent and that the connection details are correct?






I approved a SQL Data Correction for AnnuityUvIntake_PROD, but the build failed during execution with:

Login timeout expired
TCP Provider error code 0x102
A network-related or instance-specific error occurred while establishing a connection

I compared the Dev and Prod SQL Data Correction configurations and the TeamCity settings appear consistent. Since the Prod databases were recently migrated to AAG, I wanted to confirm whether doc-sqlequiflife.com is still the correct SQL endpoint/listener for AnnuityUvIntake_PROD, or if the SQL Data Correction job should now be pointing to a different AAG listener or connection target.
