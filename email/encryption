Please create the AWS DRS replication subnet and required network connectivity for MO Provider+ as part of the DR setup.

The DRS setup needs to support the following Platform Gainwell VPCs:

* MO Provider+ Tenant VPC
* Provider+ Current (N) Version VPC
* Provider+ 1 Release Back (N-1) Version VPC

Please have the Platform Gainwell team provide/confirm the VPC IDs and CIDRs for the three VPCs above.

DRS Replication CIDR: 10.134.128.0/23

Please complete the following:

* Add the DRS replication CIDR to the appropriate DR VPC.
* Create the DRS replication subnet in the same Availability Zone as an existing application subnet.
* Create a new route table for the DRS replication subnet and associate it with the new subnet.
* Establish the required VPC peering between the applicable Production VPC(s) and DR VPC for DRS replication.
* Update the DRS replication route table with the required VPC peering route.
* Update the applicable Production route tables to route DRS replication traffic to 10.134.128.0/23 through the DRS VPC peering connection.




Create AWS DRS replication subnet and VPC connectivity for MO Provider+





Hey Dennis, the new TeamCity certificate has been imported and validated successfully in NSX-ALB. I’m at the step to apply it to the TeamCity virtual service. No downtime is expected, but just wanted to confirm you’re okay with me proceeding with the certificate swap.





Hey Dennis, the new TeamCity certificate has been imported and validated successfully in NSX-ALB. I’m at the point of applying it to the TeamCity virtual service. No downtime is expected, but just wanted to confirm you’re okay with me proceeding with the certificate swap.