Update:

Identified that the November environment is not based on the Wholeshestack template, unlike other environments. Because of this, required TeamCity environment variables were not being inherited.

Action taken:
- Manually added the missing environment variables to the November configuration (including DATAOPS1-related values and other required parameters)

Result:
- The original DbOptions validation error (Host/DatabaseName null) appears to be addressed
- Deployment now progresses further than before

Next step:
@Justin – can you please confirm whether arc-hub-business is now correctly picking up these parameters in November? 

There are still failures related to admin-api (Hangfire validation), but from a DevOps perspective the missing environment variable issue should now be resolved.
