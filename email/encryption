echo "<html><body><h2>Deployed Container Images</h2><pre>" > container-info.html
docker service ls --filter name=wholeshestack_bravo --format "table {{.Name}}\t{{.Image}}" >> container-info.html
echo "</pre></body></html>" >> container-info.html






set -eu
set -o pipefail

STACK_NAME="wholeshestack_bravo"
OUT_FILE="container-info.html"

{
  echo "<html><body>"
  echo "<h2>Container Info - ${STACK_NAME}</h2>"
  echo "<pre>"
  docker service ls --filter name=${STACK_NAME} --format "table {{.Name}}\t{{.Image}}\t{{.Replicas}}"
  echo "</pre>"
  echo "</body></html>"
} > "$OUT_FILE"




set -eu
set -o pipefail

STACK_NAME="wholeshestack_bravo"
OUT_FILE="container-info.html"
SERVICES_FILE="services.txt"

if docker info 2>/dev/null | grep -q "Swarm: active"; then
  docker service ls --filter "name=${STACK_NAME}" \
    --format "table {{.Name}}\t{{.Image}}\t{{.Replicas}}" \
    > "$SERVICES_FILE"
else
  echo "Docker swarm is not active on this TeamCity agent. Unable to query deployed service/container info from this node." > "$SERVICES_FILE"
fi

{
  echo "<html><body>"
  echo "<h2>Container Info - ${STACK_NAME}</h2>"
  echo "<pre>"
  cat "$SERVICES_FILE"
  echo "</pre>"
  echo "</body></html>"
} > "$OUT_FILE"






set -eu
set -o pipefail

pushd %MkeBundlePath%
eval "$(../env.sh)"
popd

STACK_NAME="wholeshestack_bravo"
OUT_FILE="container-info.html"
SERVICES_FILE="services.txt"

if docker service ls >/dev/null 2>&1; then
  docker service ls --filter "name=${STACK_NAME}" \
    --format "table {{.Name}}\t{{.Image}}\t{{.Replicas}}" \
    > "$SERVICES_FILE"
else
  echo "Unable to query Docker Swarm service info from this TeamCity agent/deployment context." > "$SERVICES_FILE"
fi

{
  echo "<html><body>"
  echo "<h2>Container Info - ${STACK_NAME}</h2>"
  echo "<pre>"
  cat "$SERVICES_FILE"
  echo "</pre>"
  echo "</body></html>"
} > "$OUT_FILE"






set -eu
set -o pipefail

STACK_NAME="wholeshestack_bravo"
OUT_FILE="container-info.html"

{
  echo "<html><body>"
  echo "<h2>Container Info - ${STACK_NAME}</h2>"
  echo "<pre>"

  echo "Images from deployment config:"
  grep -R "image:" . | sed 's/^/  /' || echo "No image entries found."

  echo "</pre>"
  echo "</body></html>"
} > "$OUT_FILE"





set -eu
set -o pipefail

STACK_NAME="wholeshestack_bravo"
OUT_FILE="container-info.html"

{
  echo "<html><body>"
  echo "<h2>Container Info - ${STACK_NAME}</h2>"
  echo "<pre>"

  echo "Resolved Docker image tag variables:"
  grep -R "DI_.*_TAG=" . || echo "No resolved DI_*_TAG values found."

  echo ""
  echo "Image references from deployment config:"
  grep -R "image:" . | sed 's/^/  /' || echo "No image entries found."

  echo "</pre>"
  echo "</body></html>"
} > "$OUT_FILE"





set -eu
set -o pipefail

STACK_NAME="wholeshestack_bravo"
OUT_FILE="container-info.html"

{
  echo "<html><body>"
  echo "<h2>Container Info - ${STACK_NAME}</h2>"
  echo "<pre>"

  echo "Resolved Docker image tag variables:"
  grep -R "DI_.*_TAG=" . || echo "No resolved DI_*_TAG values found."

  echo ""
  echo "Image references from deployment config:"
  grep -R "image:" . | sed 's/^/  /' || echo "No image entries found."

  echo "</pre>"
  echo "</body></html>"
} > "$OUT_FILE"




set -eu
set -o pipefail

STACK_NAME="wholeshestack_bravo"
OUT_FILE="container-info.html"

{
  echo "<html><body>"
  echo "<h2>Container Info - ${STACK_NAME}</h2>"
  echo "<pre>"

  echo "Selected Docker image tags from TeamCity parameters:"
  echo "WindApi: %Windapi%"
  echo "Admin: %Admin%"
  echo "APAS-API: %APAS-API%"
  echo "Intake: %Intake%"
  echo "Portal: %Portal%"
  echo "Document-API: %Document-API%"

  echo ""
  echo "Image references from deployment config:"
  grep -R "image:" . | sed 's/^/  /' || echo "No image entries found."

  echo "</pre>"
  echo "</body></html>"
} > "$OUT_FILE"






Hey Ryan, I’ve been working through the traceability ticket for Wholeshestack-Bravo.

I was able to add a custom Container Info artifact/tab to the deployment build and confirmed the artifact is generating successfully now.

While implementing it, I realized the APAS-API Container Info is automatically generated from the Docker Registry Connections build feature because APAS is an image build pipeline, whereas Bravo is a deployment pipeline consuming already-built image tags.

Right now I can successfully generate deployment image references from the compose/deployment config, but the values are still template/parameter based unless I explicitly resolve the TeamCity deployment parameters.

Before I continue refining it, I wanted to confirm the preferred direction:

* Is the expectation simply to expose the deployment image references/tags used by Bravo deployments?
* Or do we specifically want a fully resolved APAS-style deployment image report showing the exact selected Docker image tags for each deployed service?

Also for the second part of the ticket regarding traceability, do you want an additional full Git SHA image tag published during WindApi image builds, alongside the existing generated tags?

Just wanted to validate the intended implementation before I finalize it.
