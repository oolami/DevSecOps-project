Implemented configurable environment reservation expiration and reminder functionality.

Completed:

* Updated the default environment reservation expiration from 1 week to 2 weeks (14 days).
* Added automated Microsoft Teams reminder notifications that are sent approximately 1 day before a reservation expires.
* Updated the notification workflow to post reminders to the Software Engineering - Silac Teams chat.
* Modified the reservation reminder logic to preserve the existing reservation TTL while recording reminder state, ensuring reminder notifications do not extend reservation duration.

Validation:

* Verified Teams reminder notifications are delivered successfully.
* Verified reservation TTL is preserved after reminder notifications.
* Reservation expiration remains automatic based on the configured TTL.