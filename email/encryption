Hi Erik,

As part of DR readiness for the upcoming exercise, I completed a parity review of several IAM instance profiles between PROD and DR. Based on the comparison (PROD on the left, DR on the right in the attached screenshots), there are multiple missing permission policies in DR that need to be aligned with PROD.

Please see the gaps identified below:

1. Role: customer-pr-mc-ec2-instance-profile

Missing in DR:

AmazonSSMDirectoryServiceAccess
AmazonSSMManagedInstanceCore
BYOIPSSMPolicy
CloudWatchAgentServerPolicy
gms_ds_auto_join_domain_policy
pfe-pg--gbl-* (platform policy)

2. Role: customer-mc-ec2-instance-profile-s3-custom

Missing in DR:

AmazonSSMDirectoryServiceAccess
AmazonSSMManagedInstanceCore
BYOIPSSMPolicy
CloudWatchAgentServerPolicy
gms_ds_auto_join_domain_policy
pfe-pg--gbl-* (platform policy)

3. Role: customer-mc-ec2-instance-profile

Missing in DR:

AmazonSSMDirectoryServiceAccess
AmazonSSMManagedInstanceCore
BYOIPSSMPolicy
CloudWatchAgentServerPolicy
gms_ds_auto_join_domain_policy
pfe-pg--gbl-* (platform policy)

To ensure full parity and prevent any permission-related issues during the DR exercise, could you please add the above missing policies to the corresponding roles in the DR account?

Let me know once completed, and I will re-validate.
